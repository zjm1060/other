/**************************************************************************************** 
说明:
    应用程序用户自定义文件。
    
    SID: 		在本机所建的远程历史数据库本地服务名,默认为 HISTA 
    UPDATEFIRST:	hdbwrite启动时,是否将历史库实时表数据全部更新:
			1，全部更新；0，不更新
    WRANYHOST:		hdbwrite及curevewrite是否对主机状态进行判断.1，不判断主机状态，
			即任何主机只要运行程序即可往历史库写数；0，判断主机状态，只有主控
			时才写库。内网应设置为0，外网设置为1.	
    WRCOND		是否判断写库条件。 1，判断写库条件； 0，不判断写库条件
    WROFFSET		是否判断历史库死区。 1，判断死区； 0，不判断死区			
    CPUNETMODE:		网络配置类型。4:双机真双网；3:双机单网；2：单机双网；1：单机单网；
    			5：双机双网，但只有两个网络模块，同时连在主CPU上。	
    SYNCTIME:		是否需要由上位机给PLC对时. 0: 不需要; 1:需要
    SIMUMODE:		1: 仿真模式   0:非仿真模式    
  
*****************************************************************************************/

BEGIN

SID		hista

UPDATEFIRST	0
WRANYHOST	0
WRCOND		1
WROFFSET	1
CPUNETMODE	3
SYNCTIME	0
SIMUMODE	1

END

